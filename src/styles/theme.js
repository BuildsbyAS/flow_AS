import { color, space, radius, shadow, font, type, motion } from './ds.js';

export const theme = { color, space, radius, shadow, font, type, motion };
export { color, space, radius, shadow, font, type, motion };
